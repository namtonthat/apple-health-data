{
  type:  {
    provider: "aws",
    region: "ap-southeast-2"
  },
  input: {
  # absolute path that defines the csv file location that were exported from the iPhone
    raw_path: '/Users/ntonthat/Library/Mobile Documents/iCloud~com~ifunography~HealthExport/Documents/raw/'
  },
  output: {
  #  absolute path that defines the local storage to output to
    raw_path: "/Users/ntonthat/Documents/scratch/apple-health-calendar/outputs",
  # output_cal defines the public storage to output to after it has been output locally
    calendar_path: "s3://ntonthat-ahc",
    file_name: "apple-health-calendar"
  },
  col_map: {
        'Date': 'date',
        'Apple Exercise Time (min)': "exercise_mins",
        'Carbohydrates (g)': 'carbs',
        'Fiber (g)': 'fibre',
        'Mindful Minutes (min)': "mindful_mins",
        'Protein (g)': 'protein',
        'Total Fat (g)': 'fat',
        'Sleep Analysis [In Bed] (hr)': 'sleep_in_bed',
        'Sleep Analysis [Asleep] (hr)': 'sleep_asleep',
        'Step Count (count)': 'steps',
        'Weight & Body Mass (kg) ': 'body_weight'
  },

}