# =============================================================================
# Health & Fitness Data Pipeline - Secrets
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
#
# Non-sensitive config (bucket name, goals, URLs) is in pyproject.toml
# under [tool.dashboard].
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Credentials (REQUIRED)
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# Hevy API (OPTIONAL - for workout tracking)
# -----------------------------------------------------------------------------
HEVY_API_KEY=

# -----------------------------------------------------------------------------
# Strava API (OPTIONAL - for cardio activities)
# -----------------------------------------------------------------------------
# Create an app at https://www.strava.com/settings/api
# Then run ./scripts/strava-auth.sh to get tokens
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=

# -----------------------------------------------------------------------------
# Pipeline Settings
# -----------------------------------------------------------------------------
# These duplicate pyproject.toml values for pipelines that read from env vars
S3_BUCKET_NAME=your-bucket-name
AWS_DEFAULT_REGION=ap-southeast-2

# Directory for dlt pipeline state
DLT_PIPELINE_DIR=.dlt_pipelines

# Path for local DuckDB database (used by dbt)
DBT_DUCKDB_PATH=hevy.duckdb
