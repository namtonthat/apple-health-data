# =============================================================================
# Health & Fitness Data Pipeline - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Hevy API
# -----------------------------------------------------------------------------
# Get your API key from Hevy app settings or developer portal
HEVY_API_KEY=

# -----------------------------------------------------------------------------
# AWS Credentials
# -----------------------------------------------------------------------------
# Option 1: Explicit credentials (recommended for this project)
# Create an IAM user with S3 read/write permissions
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Option 2: Use AWS CLI profile instead (comment out above, uncomment below)
# AWS_PROFILE=health-pipeline

# AWS Region for S3 bucket
AWS_DEFAULT_REGION=ap-southeast-2

# -----------------------------------------------------------------------------
# S3 Configuration
# -----------------------------------------------------------------------------
# Bucket name (without s3:// prefix)
# Data will be organized as:
#   s3://{bucket}/landing/  - Raw extracted data
#   s3://{bucket}/raw/      - Cleansed data (snake_case, metadata)
S3_BUCKET_NAME=

# -----------------------------------------------------------------------------
# Pipeline Settings
# -----------------------------------------------------------------------------
# Directory for dlt pipeline state (tracks incremental loads)
DLT_PIPELINE_DIR=.dlt_pipelines

# -----------------------------------------------------------------------------
# dbt Settings
# -----------------------------------------------------------------------------
# Path for local DuckDB database (used by dbt)
DBT_DUCKDB_PATH=hevy.duckdb
